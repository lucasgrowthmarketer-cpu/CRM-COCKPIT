
# Archives de release — générées, pas versionnées
*.zip

# Données sensibles RGPD (emails de prospects)
cockpit/data/*.xlsx
!cockpit/data/.gitkeep

# Logs temporaires
/tmp/uvicorn.log

# Source d'intégration — ne pas commit
cockpit_full/
CRM-COCKPIT-backup-*/
cockpit_v2/
*.before-tasks-plaquette

# Backups locaux des fichiers avant migration tasks/plaquette
*.before-tasks-plaquette

# Frontend env local (URL Codespaces, ne pas commit)
cockpit/frontend/.env.local
facturation_module/
*.before-facturation
